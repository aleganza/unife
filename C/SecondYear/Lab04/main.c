#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    
    int fd;
    char buff[1024]

    // controllo argomenti
    if (argc != 2) {
        printf("Usage: a.out <nomefile>");
        exit(1);
    }

    // file
    

    printf("\n");
    return 0;
}
